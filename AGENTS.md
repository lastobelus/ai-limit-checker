## Plan Storage
- Save all generated plans in the repository under `plans/`.
- Do not write plans to `~/.codex/plans` or `~/.code/plans` for this project.
- If a helper tool defaults to home-directory plan storage, bypass it and write plan files directly into `plans/`.
